# Backend API Configuration
# The base URL for all API requests to the Laravel backend
# Must include the /api/v1/ path and use HTTPS in production
NUXT_PUBLIC_API_BASE=https://admin.mumuso.com.bd/api/v1/

# Session Encryption Password
# Used to encrypt session data (guest cart, auth tokens, etc)
# Must be at least 32 characters in production
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
NUXT_SESSION_PASSWORD=42fb3ccd4d6f4df49f4429771b02141f
